<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/toolbar_scan"
        android:title="@string/toolbar_scan"
        app:showAsAction="never"/>

    <item
        android:id="@+id/toolbar_clear"
        android:title="@string/toolbar_clear"
        app:showAsAction="never"/>

    <item
        android:id="@+id/toolbar_multiselect"
        android:title="@string/toolbar_multiselect"
        app:showAsAction="never"/>

    <item
        android:id="@+id/toolbar_search"
        android:title="@string/toolbar_search"
        app:showAsAction="never"/>

</menu>